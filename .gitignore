# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# docs IA & AI Tools
.agent/
.agent-teams/
.ai-tools/
.atl/
.codex/
.cursor/
.engram/
.gemini/
.guardian/
.kilocode/
.opencode/
.qoder/
designInspo/

.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v4/
clone.html
checkout_diff.txt
*.backup

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
playwright-report/
test-results/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
.env.prod
# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.aider*
.claude/


# NOTE: keep *.txt files tracked by default. If you previously ignored a specific
# .txt file (e.g. `tasks.txt`) and want it tracked, remove it from this list.
*.txt

# Sentry Config File
.env.sentry-build-plugin

# AI debug/dump files (transcript recovery, pattern dumps, etc.)
dump.*
found.*
raw_*
recover.*
recovered_*
search_history.*

# IDE & MCP Meta
.mcp.json
opencode.json

# --------------------------------------------------
# Ignore Markdown files by default but allow a short whitelist
# (use this to avoid committing auto-generated analysis files)
*.md
!README.md
!SOUL.md
!AGENTS.md

# Explicitly ignore analysis files created in the workspace
informe_detallado_archivos_grandes.md
tareas_prs_prioritizadas.md

# If you prefer an alternate naming convention for local notes, consider
# using a suffix not matching .md (e.g. .task or .private) and add patterns
# here as needed.
.env.local
send-test-email.ts
.env.current

sendTestEmail.ts
